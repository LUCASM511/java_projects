package org.main;

class Maath {
	// find the lowest
	Math.min(5, 10);

	// find the highest
	Math.max(2, 1);

	// returns the square root
	Math.sqrt(64);

	// returns the absolute
	Math.abs(-4.7);
	
	// returns a random number betwen 0.0 (inclusive), and 1.0 (exclusive)
	Math.radom();

	// more control over the random number
	int random = (int)(Math.random() * 101); // 0 to 100
}

